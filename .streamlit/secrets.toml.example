# Copiar a secrets.toml (local) o pegar en Streamlit Cloud → Settings → Secrets
# Generar una llave nueva con:
#   python -c "from cryptography.hazmat.primitives.asymmetric import ec; print(format(ec.generate_private_key(ec.SECP256R1()).private_numbers().private_value,'064x'))"

LLAVE_PRIVADA = "REEMPLAZAR_CON_HEX_DE_64_CARACTERES"
