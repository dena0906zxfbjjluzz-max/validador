# --- Acceso planta ---
[credenciales]
usuario = "su_usuario"
clave = "su_clave_secreta"

# --- Sello Ed25519 ---
LLAVE_PRIVADA = "REEMPLAZAR_CON_HEX_DE_64_CARACTERES"

# --- Supabase (nombres EXACTOS en minúsculas) ---
SUPABASE_URL = "https://xxxx.supabase.co"
SUPABASE_KEY = "eyJhbGciOi..."   # preferible service_role para bypassear RLS

# SQL (Table Editor → SQL):
#
# create table if not exists public.historial_reportes (
#   id bigint generated always as identity primary key,
#   fecha text not null,
#   lote text not null,
#   hash_sha256 text unique not null,
#   inspector text not null,
#   created_at timestamptz default now()
# );
#
# -- Si usa anon key, habilite INSERT:
# alter table public.historial_reportes enable row level security;
# create policy "insert sellos" on public.historial_reportes
#   for insert to anon, authenticated with check (true);
