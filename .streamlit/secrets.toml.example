# --- Acceso planta (obligatorio en Cloud) ---
[credenciales]
usuario = "su_usuario"
clave = "su_clave_secreta"
# nombre_planta = "Planta Autorizada"

# --- Sello Ed25519 ---
LLAVE_PRIVADA = "REEMPLAZAR_CON_HEX_DE_64_CARACTERES"

# --- Supabase: réplica automática al firmar un lote ---
SUPABASE_URL = "https://xxxx.supabase.co"
SUPABASE_KEY = "eyJhbGciOi..."

# SQL recomendado en Supabase (Table Editor → SQL):
#
# create table if not exists historial_reportes (
#   id bigint generated always as identity primary key,
#   fecha text not null,
#   lote text not null,
#   hash_sha256 text unique not null,
#   inspector text not null,
#   fecha_hora text,
#   responsable text,
#   created_at timestamptz default now()
# );
#
# -- Políticas: habilite insert con la service role key, o RLS con policy de insert.
